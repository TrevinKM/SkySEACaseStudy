pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                dir('GetYourWay/src/main/gywWebApp') {
                    sh "npm install"
                    sh "npm run build"
                }
            }
        }
        stage("Deploy") {
            dir('GetYourWay/src/main/gywWebApp') {
                steps {
                    sh 'serve -s build'
                }
            }
        }
    }
}